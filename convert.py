import getRate as gr

#real time conversion of amount from one currency to another
#similar to compareCost but uses google payment api which is implemented on android and cannot be used on command line
def convert():
	print("Cannot be implemented on Command Line")
	pass
	#It is functional on the android application only
